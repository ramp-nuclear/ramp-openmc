[pytest]
markers = 
    regression: Regression tests. Should fail when changes are desired.
    slow: Slow tests that should not be run when moving fast.
    xs: Tests that require access to cross sections, which may not exist on your machine.
